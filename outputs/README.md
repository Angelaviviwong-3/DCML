# Generated output

The defaults are `figures/`, `enhance/`, `prediction/`, `logs/`, and `demo/`.
Dataset-specific nuisance and causal outputs retain their locations under
`data/processed/<dataset>/`. Output contents are ignored by Git.
