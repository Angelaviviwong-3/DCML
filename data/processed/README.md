# Processed data

This is the default location for dataset tables, intermediate artifacts, and
dataset-specific DML results. See [required contracts](../../docs/DATA.md).
The scripts retain historical artifact suffixes. All generated files here are ignored by Git.
